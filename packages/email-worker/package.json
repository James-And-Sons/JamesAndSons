{
  "name": "jas-email-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build step required for email-worker'",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
